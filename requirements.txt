# Core Web Server (Flask)
Flask>=3.0.0
flask-cors>=4.0.0

# Optional FastAPI / Uvicorn backend
fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.0.0

# Optional Production WSGI Server for Linux/macOS
gunicorn>=21.2.0; sys_platform != 'win32'
