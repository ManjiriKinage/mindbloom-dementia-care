version: "3.8"

services:
  mindbloom:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mindbloom_app
    restart: unless-stopped
    ports:
      - "${PORT:-5000}:5000"
    environment:
      - HOST=0.0.0.0
      - PORT=5000
      - FLASK_DEBUG=0
